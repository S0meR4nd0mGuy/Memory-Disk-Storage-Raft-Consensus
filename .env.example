# Node Configuration
NODE_ID=node1
LISTEN_ADDR=0.0.0.0
LISTEN_PORT=5000

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000

# Raft Configuration
RAFT_MIN_ELECTION_TIMEOUT_MS=150
RAFT_MAX_ELECTION_TIMEOUT_MS=300
RAFT_HEARTBEAT_INTERVAL_MS=50

# Storage Configuration
STORAGE_TYPE=memory  # Options: memory, lsm, btree
STORAGE_DATA_DIR=./data

# Logging
LOG_LEVEL=INFO  # Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
